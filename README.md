# srs-practice-test-login-v1-0-0-2026-04-21-page-1

Auto-generated Playwright framework for SRS — Practice Test Login | v1.0.0 | 2026-04-21 Page 1.

## Quick start
```bash
npm install
npm run install:browsers
cp .env.example .env   # then edit credentials if needed
npm test
```

## Credentials
Tests read credentials from environment variables (see `.env.example`)
or fall back to values baked into `fixtures/test-data.json`.
